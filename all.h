/* All Includes File - stub math funcs */
char Lowercase(char c) {
    return c<96 ? c+32 : c;
}

int Max(int x1,int x2) {
    return x1>=x2 ? x1 : x2;
}

int Min(int x1, int x2) {
    return x1<=x2 ? x1 : x2;
}